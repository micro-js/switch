{
  "name": "@f/switch",
  "description": "A functional switch statement.",
  "repository": "git://github.com/micro-js/switch.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/noop": "^1.2.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
